{
  "tagFormat": "${version}",
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/changelog", {
      "changelogFile": "CHANGELOG.md"
    }],
    ["@semantic-release/exec", {
      "prepareCmd": "sed -i 's/^version:.*/version: ${nextRelease.version}/;s/^appVersion:.*/appVersion: ${nextRelease.version}/' charts/bebot/Chart.yaml && sed -i 's/  imageTag: .*/  imageTag: \"${nextRelease.version}\"/' charts/bebot/values.yaml"
    }],
    ["@semantic-release/git", {
      "assets": ["charts/bebot/Chart.yaml", "charts/bebot/values.yaml", "CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]"
    }],
    ["@semantic-release/github", {
      "successComment": false,
      "failTitle": false
    }]
  ]
}
